# Fable 가이드

## 지도와 영토

- Fable 모델을 활용한 작업에서 핵심은, 사용자가 모델에게 주는 `지도`와 작업이 실제 일어나는 `영토`의 간극인 `미지(unknowns)`를 찾아 좁히는 것
    - 지도: 내가 모델에게 주입하는 것
        - 프롬프트, 스펙, 스킬, …
    - 영토: 실제로 작업이 일어나는 곳
        - 코드베이스, 현실, 제약, …
- 지도가 아무리 정성스럽더라도, 영토와 동일할 순 없음
    - 이러한 간극이 발생할때, AI는 추측해서 결정함 → 내가 결정한 적 없는 결정 지점
    - 맡긴 일이 클수록, 많은 코드베이스를 확인해야할 수록 미지의 발생 가능성은 높아짐

## Fable

- 미지를 명확히 하는 사용자 능력에 의해 작업 품질이 좌우되는 모델
- 미지를 발견하면, Claude는 최선의 추측으로 결정
- 즉, 미지를 명확히 하지 못하면 병목이 생긴다

## 미지 알기

- 문제를 4가지로 분해
    - `Known Knowns`: 프롬프트에 담긴 것, 에이전트에게 원한다고 말하는 것
    - `Known Unknowns`: 아직 파악하지 못했으나 못했다는 사실은 인지하는 것
    - `Unknown Knowns`: 너무 당연해 적어두지 않지만 보면 알아보는 것
    - `Unknown Unknowns`: 전혀 고려하지 않은 것, 인지하지 못한 지식, 얼마나 좋아질 수 있는지 모르는 것 ⭐
        - 빨리 알면 알수록 좋은 것

## 미지 찾아내는 방법

### 1단계. 구현하기 전

1. 사각지대 점검
    - Claude에게 unknown unknowns를 찾아 설명해 달라고 요청하기
    - Claude에게 내가 누구이며 무엇을 알고있는지 컨텍스트를 제공
    - 예시 프롬프트
        - "새 auth provider 추가 작업 중인데 이 코드베이스의 auth 모듈을 전혀 모름. blindspot pass로 관련 unknown unknowns를 파악하고 더 잘 프롬프트하도록 도와달라"
        - "color grading이 뭔지 모르지만 이 영상을 grade해야 함. 더 잘 프롬프트하도록 color grading의 unknown unknowns를 이해하게 가르쳐달라"
2. 브레인스토밍과 프로토타입
    - 보면 알지만 미리 정의하기 어려운 unknown knowns를 찾기
    - Claude와 보면 아는데 정의하기 어려운 기준을 찾아 프로토타입 만들어보며 결정하기
    - 구현을 시작하기 전 범위를 정의하여, 적당한 범위를 찾기
    - 예시 프롬프트
        - "이 데이터용 대시보드를 원하지만 시각적 감각이 없고 뭐가 가능한지 모름. 완전히 다른 4가지 디자인 방향의 HTML 페이지를 만들어 반응할 수 있게 해달라"
        - "연결 작업 전에 가짜 데이터로 새 에디터 툴바를 목업한 단일 HTML 파일을 만들어달라. 실제 앱을 건드리기 전 레이아웃에 반응하고 싶음"
        - "대략적 문제는 온보딩 후 사용자 이탈. 코드베이스를 검색해 가장 저렴한 것부터 가장 야심찬 것까지 개입 가능한 10곳을 브레인스토밍해달라"
3. 인터뷰
    - 거꾸로 나를 취조하듯 질문하게 만들기
    - 충분한 브레인스토밍 후에도 남은 미지가 있다면, 이 미지에 대한 인터뷰를 요청하고 질문을 유도
    - 질문은 나의 답에 따라 설계가 통째로 바뀌어버리는 핵심적인 것부터 시작하도록 질문 순서 지정
    - 예시 프롬프트
        - "모호한 부분을 한 번에 하나씩 질문하고, 내 답이 아키텍처를 바꿀 질문을 우선하라"
4. 레퍼런스
    - 내가 원하는 바에 해당하는 레퍼런스를 전달하기
    - 예시 프롬프트
        - "vendor/rate-limiter의 이 Rust crate가 원하는 backoff 동작을 정확히 구현함. 읽고 같은 의미를 우리 TypeScript API 클라이언트에 재구현하라"

### 2단계. 구현 중

1. 구현 노트
    - 아무리 계획하더라도 unknown unknowns는 항상 잠복. 작업 중 발견한 엣지 케이스로 인해 다른 방식을 취해야할 수도 있음
    - 작업을 메모하고 작업중 내린 결정들을 기록하게 함 → 다른 프로젝트의 지도를 만드는 과정
    - 예시 프롬프트
        - "implementation-notes.md 파일을 유지하라. 계획에서 벗어나게 하는 엣지 케이스를 만나면 보수적 선택을 하고 'Deviations'에 기록한 뒤 계속 진행하라"

### 3단계. 구현 후

1. 퀴즈
    - 이번 변경을 내가 이해했는지 확인하는 시간
    - AI가 만든 코드를 내 것으로 만드는 과정
    - 예시 프롬프트
        - "이 변경에서 일어난 모든 것을 이해하고 싶음. 컨텍스트·직관·수행 내용 등을 담은 HTML 리포트와 반드시 통과해야 할 퀴즈를 하단에 달아달라"

## 지도와 영토 맞추기

- 모델이 좋아질수록 올바른 접근으로 더 많은 것을 달성 가능하며, 장기 과제가 잘못 돌아오면 대개 미지 정의나 Claude가 즉흥 대응할 수 있는 구현 계획에 더 시간을 써야 함
- 모든 설명·브레인스토밍·인터뷰·프로토타입·레퍼런스는 문제가 비싸지기 전에 저렴하게 미지를 발견하는 수단
- 다음 프로젝트는 Claude에게 미지 찾기를 요청하며 시작하는 것이 핵심

## 출처

- https://news.hada.io/topic?id=31107
- [https://www.youtube.com/watch?v=vZaG9KlT-40](https://www.youtube.com/watch?v=vZaG9KlT-40)